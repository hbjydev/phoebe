---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmproxy_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMProxy
metadata:
  name: litellm
spec:
  # renovate: datasource=docker depName=ghcr.io/berriai/litellm
  image: ghcr.io/berriai/litellm:v1.99.0-rc.2@sha256:497515eb96640c28f223071c82e9cbc04af37f9e64897e652b4e45754194de13
  replicas: 1

  generalSettings:
    health_check_endpoint: /v1/health

  litellmSettings:
    success_callback: ["prometheus"]
    prometheus_adapter: true
    cache: true
    cache_params:
      type: redis
      host: os.environ/REDIS_HOST
      port: os.environ/REDIS_PORT
    drop_params: true
    merge_reasoning_content_in_choices: true
    set_verbose: false
    require_auth_for_metrics_endpoint: false

  podAnnotations:
    reloader.stakater.com/auto: "true"

  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: litellm

  volumeMounts:
    - name: data
      mountPath: /app/chatgpt_tokens
      subPath: chatgpt-tokens

  envFrom:
    - secretRef:
        name: litellm-secret

  env:
    - name: CHATGPT_TOKEN_DIR
      value: /app/chatgpt_tokens

    - name: DATABASE_URL
      valueFrom:
        secretKeyRef:
          key: litellm-postgres
          name: url

    - name: LITELLM_LOG
      value: INFO

    - name: REDIS_HOST
      value: dragonfly.database.svc

    - name: REDIS_PORT
      value: "6379"

  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 4Gi

  service:
    type: ClusterIP
    port: 4000

  route:
    hostnames:
      - llm.hayden.moe
    parentRefs:
      - name: external
        namespace: network
