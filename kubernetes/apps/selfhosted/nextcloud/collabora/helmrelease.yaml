---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud-collabora
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      collabora:
        containers:
          app:
            image:
              repository: collabora/code
              tag: 25.04.9.4.1@sha256:fe49c08c27e102c1877ad4ef2ac1a7bd8ccd2dfaa6641d4533389c204a92bb88
            env:
              extra_params: --o:ssl.termination=true --o:ssl.enable=false --o:security.capabilities=false
              aliasgroup1: https://drive.hayden.moe:443
              domain: drive\\.hayden\\.moe
            resources:
              requests:
                cpu: 150m
                memory: 768Mi
              limits:
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: [ALL] }

    service:
      app:
        ports:
          http:
            port: 9980

    route:
      app:
        hostnames:
          - collabora.hayden.moe
        parentRefs:
          - name: internal
            namespace: network
