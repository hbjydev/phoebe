---
apiVersion: kaniop.rs/v1beta1
kind: KanidmGroup
metadata:
  name: ${APP}-admins
spec:
  kanidmRef:
    name: kanidm
    namespace: security
  kanidmName: ${APP}_admins
  accountPolicy:
    authSessionExpiry: 2678400
  members: ${OIDC_ADMIN_GROUP_MEMBERS:=${emptyArray}}
