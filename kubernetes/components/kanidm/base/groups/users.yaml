---
apiVersion: kaniop.rs/v1beta1
kind: KanidmGroup
metadata:
  name: ${APP}-users
spec:
  kanidmRef:
    name: kanidm
    namespace: security
  kanidmName: ${APP}_users
  accountPolicy:
    authSessionExpiry: 2678400
  members: ${OIDC_USERS_GROUP_MEMBERS:=${emptyArray}}
